target/
**/build/
dist/
*.pdb
**/*.rs.bk
.env
node_modules/
# Playwright MCP scratch directory
.playwright-mcp/
# Playwright test artifacts
ui/tests/test-results/
ui/tests/playwright-report/
# Stray screenshot from earlier investigation sessions
/makefile.png
# Local design handoff bundles — kept out of git per scoping decision.
/design/
# Per-worktree isolated freenet local node state (run-node task).
/.run-node/
