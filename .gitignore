target/
**/build/
dist/
*.pdb
**/*.rs.bk
.env
node_modules/
# Playwright MCP scratch directory
.playwright-mcp/
# Playwright test artifacts
ui/tests/test-results/
ui/tests/playwright-report/
# Stray screenshots from earlier investigation / QA sessions
/makefile.png
/*.png
# Ad-hoc Claude workflow scripts (regenerable, per-session scratch).
/.claude/wf-*.js
# Local design handoff bundles — kept out of git per scoping decision.
/design/
# Per-worktree isolated freenet local node state (run-node task).
/.run-node/
# Claude Code scheduled-task runtime lock (per-worktree).
/.claude/scheduled_tasks.lock
# Claude Code worktrees parent dir (each subdir is a separate git worktree).
/.claude/worktrees/
